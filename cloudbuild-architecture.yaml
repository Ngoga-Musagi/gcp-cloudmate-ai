steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [
    'build',
    '-f', 'Dockerfile.architecture',
    '-t', 'gcr.io/$PROJECT_ID/architecture-agent:$BUILD_ID',
    '-t', 'gcr.io/$PROJECT_ID/architecture-agent:latest',
    '.'
  ]
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/architecture-agent:$BUILD_ID']
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/architecture-agent:latest']
- name: 'gcr.io/cloud-builders/gcloud'
  args: [
    'run', 'deploy', 'architecture-agent',
    '--image', 'gcr.io/$PROJECT_ID/architecture-agent:latest',
    '--platform', 'managed',
    '--region', 'us-central1',
    '--allow-unauthenticated',
    '--memory', '2Gi',
    '--cpu', '2',
    '--max-instances', '10',
    '--timeout', '3600'
  ]
