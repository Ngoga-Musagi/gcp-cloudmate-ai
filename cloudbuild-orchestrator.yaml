steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [
    'build',
    '-f', 'Dockerfile.orchestrator',
    '-t', 'gcr.io/$PROJECT_ID/orchestrator-agent:$BUILD_ID',
    '-t', 'gcr.io/$PROJECT_ID/orchestrator-agent:latest',
    '.'
  ]
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/orchestrator-agent:$BUILD_ID']
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/orchestrator-agent:latest']
- name: 'gcr.io/cloud-builders/gcloud'
  args: [
    'run', 'deploy', 'orchestrator-agent',
    '--image', 'gcr.io/$PROJECT_ID/orchestrator-agent:latest',
    '--platform', 'managed',
    '--region', 'us-central1',
    '--allow-unauthenticated',
    '--port', '8001',
    '--memory', '2Gi',
    '--cpu', '2',
    '--max-instances', '10',
    '--timeout', '3600'
  ]
